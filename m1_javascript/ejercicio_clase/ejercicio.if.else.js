/*1. Uso de la estructura if-else
1. Crea una variable numero y asígnale un número entero.
2. Escribe una estructura if-else que muestre en consola si el número es positivo, negativo o cero
*/
let number = -7;
if (number > 0) {
    console.log('${number'} es un numero positivo);
}
else if (number > 0) {
    console.log('${number'} es un numero negativo);
}
else if (number > 0) {
    console.log('${number'} es un numero negativo);
}


let edad = undefined
function esMayorDeEdad(edad) {
    return edad >= 18;
}

let edad = null;
console.log(esMayorDeEdad)

//compobar si el salario esta comprendido entre 28000 y 34000
let salary = 76000.0
const MIN_SALARY = 28000;
const MAXI_SALARY = 34000;
if (salary >= MIN_SALARY && salary <= MAXI_SALARY) {

}